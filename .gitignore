# managed by template (do not remove)
.env
.env.bak
compose.yaml
setup.conf.bak
setup.conf.local
coverage/
.Dockerfile.generated
.docker.xauth

# SDK Manager local data (login session + download cache)
.data/
