# AGX Orin devkit — flash to M.2 NVMe SSD.
# See _example.yaml for the canonical schema with all options.

jetpack:
  version: "6.2.2"

hardware:
  board: agx-orin

storage:
  device: nvme

user:
  username: jetson
  password: jetson
  hostname: jetson-agx
  autologin: false
