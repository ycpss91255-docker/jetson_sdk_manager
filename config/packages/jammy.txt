# GUI-only X11 / audio client libs. The Electron renderer deps shared by both
# cli and gui (libgl1-mesa-dri + dbus-x11) live in the sdkm-base stage now, so
# the cli renderer is healthy too (#87) — not just the gui.
libasound2
libcanberra-gtk3-module
